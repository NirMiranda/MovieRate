
beforeAll(done => {
    done()
})
afterAll(done => {
    done()
})
describe("auth test", () => {
    test("This is initial test pass 1", async () => {
        const temp = 2
        expect(temp).toEqual(2)
    });
})
// const request= require("supertest");
// const initApp= require("../app");
// const mongoose = require('mongoose');
// const User=require('../models/user_model')

