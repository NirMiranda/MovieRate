
beforeAll(done => {
    done()
})
afterAll(done => {
    done()
})
describe("auth test", () => {
    test("This is initial test pass 1", async () => {
        const temp = 2
        expect(temp).toEqual(2)
    });
})
